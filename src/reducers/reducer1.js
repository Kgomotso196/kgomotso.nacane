// reducer1.js
const initialState = {
    // Initial state for reducer1
  };

  const reducer1 = (state = initialState, action) => {
    switch (action.type) {
      // Define cases to handle actions
      default:
        return state;
    }
  };

  export default reducer1;
